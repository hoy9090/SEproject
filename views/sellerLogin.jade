extends layout

block style
  style.
    label.hvr-skew-backward {margin-bottom: 2em; width: 100%; text-align: center;}
    a.hvr-skew-backward {margin-bottom: 50px; width: 100%; text-align: center; cursor: pointer;}

block content
  script.
    window.fbAsyncInit = function() {
    FB.init({
      appId      : '1725103691081244',
      cookie     : true,  // enable cookies to allow the server to access 
                          // the session
      xfbml      : true,  // parse social plugins on this page
      version    : 'v2.6' // use version 2.6
    });
    };

    function go_signup(){
      FB.getLoginStatus(function(response) {
        if (response.status == 'connected') {
          FB.api(
            '/me',
            'GET',
            {"fields":"id,name,gender,email"},
            function(response) {
              $.ajax({
                type: 'POST',
                data: response, 
                dataType: "JSON",
                url: '/sign_buyerFB',
                success: function(data) {
                  var form = document.createElement('form');
                  form.setAttribute('method', 'post');
                  form.setAttribute('action', '/sign_buyer');
                  for (var key in data) {
                    var hiddenField = document.createElement("input");
                    hiddenField.setAttribute("type", "hidden");
                    hiddenField.setAttribute("name", key);
                    hiddenField.setAttribute("value", data[key]);
                    form.appendChild(hiddenField);
                  }
                  document.body.appendChild(form);
                  form.submit();
                },
                error: function(request, status, error) {
                  location.href = document.referrer;
                }
              });
            });
          }
        });
      }

      // Load the SDK asynchronously
      (function(d, s, id) {
        var js, fjs = d.getElementsByTagName(s)[0];
        if (d.getElementById(id)) return;
        js = d.createElement(s); js.id = id;
        js.src = "//connect.facebook.net/en_US/sdk.js";
        fjs.parentNode.insertBefore(js, fjs);
      }(document, 'script', 'facebook-jssdk'));

  .container
    .login
      .col-md-6.login-title
          h3 Log-in page for Seller !
      .col-md-6.login-title
          p
            | Log-in page for Seller !
      form(role='form', action='login_check', method='post')
        .col-md-6.login-do
          .login-mail
            input(type='text', name='email', placeholder='Email', required)
            i.glyphicon.glyphicon-envelope
          .login-mail
            input(type='password', name='pw', placeholder='Password', required)
            i.glyphicon.glyphicon-lock
          //a.news-letter(href='#')
            label.checkbox1
              input(type='checkbox', name='checkbox')
              i  
              | Forget Password
          label.hvr-skew-backward
            input(type='submit', value='Login for Seller')
        .col-md-6.login-right
          h3 Join Board a To z !
          p
            | Hello. Our site is on-line board shopping mall and community site. Especially, our site offer customizing feature as well as selling complete boards. So, in our site, there are potential customers. Join Board a TO z and enjoy our site. 
          h5 ..
          a.hvr-skew-backward(href='sign') Register
        .clearfix

block script
  if (login_fail)
    script.
      alert('Verify your email or password.');
